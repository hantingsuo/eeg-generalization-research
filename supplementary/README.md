# Supplementary machine-readable resource

`ESM_2.json` is Online Resource 2 for the accompanying manuscript. It records the final CF-TRE component order, selected mean- and tail-risk parameters, full-precision mixture weights, and source checksum.

The file contains aggregate configuration information only. It does not contain participant-level data or predictions.
